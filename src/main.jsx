import React from 'react'
import ReactDOM from 'react-dom/client'
import AppWithDB from './AppWithDB.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppWithDB />
  </React.StrictMode>,
)
